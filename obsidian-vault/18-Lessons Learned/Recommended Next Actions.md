# Recommended Next Actions

Last updated: 2026-08-29

## Purpose

This note continuously converts current project state, risks, debt, lessons, and metrics into recommended next actions.

Related: [[Project Metrics]], [[Improvement Backlog]], [[Optimization Opportunities]], [[Critical Path]], [[Recommended Next Actions]], [[Project Memory]].

## Current Recommendation Summary

Governance update 2026-06-05: the root operating manual and Obsidian AI operating notes encode the Rain Autonomous Engineering System source-of-truth priority order, reality-enforcement rule, and workflow node state machine. The vault also has [[Scenario Intelligence Agent]], [[System Model]], [[State Graph]], [[Business Rule Graph]], [[Assumption Register]], [[Failure Graph]], and [[Scenario Coverage Matrix]] so testing/intelligence agents can generate scenarios from explicit models. Senior audit Phase 9 now adds semantic vault validation for operational owner, priority, evidence, review-date, fixed-status, closed-blocker, and P0/P1 next-action fields; the remaining governance hardening is generated metric reconciliation and release-artifact evidence automation.

Use [ROOT_CAUSE_ANALYSIS.md](../../ROOT_CAUSE_ANALYSIS.md) as the current evidence lock for the call/presence/update/diagnostics failure cluster. Mitigations completed so far: late received voice signaling frames after terminal rooms no longer become crash records, Firebase terminal room writes no longer depend on callee inbox rows, Android SAF document handles no longer get opened as raw filesystem paths, stale raw-online backend presence no longer seeds or starts user-facing actions, failed call setup diagnostics now include Firebase room status timelines, update checks no longer report stale Remote Config policy as "up to date," Phase 05 presence routing now uses shared `online + heartbeat + state` freshness for UI Connect and auto-recovery, 2026-06-04 terminal-sensitive local media signaling writes now preflight terminal rooms before `writeVoiceOffer`/`writeVoiceAnswer`, and 2026-06-04 terminal call cleanup now publishes failed/idle UI state before bounded WebRTC/session cleanup. Diagnostics export now also creates a real fallback JSON file for Android content URI or `/document/...` picker handles, and 2026-06-05 Phase 4 routes crash diagnostics/debug logs/final exports through a central recursive sanitizer. Phase 08 plus the terminal cleanup pass added targeted local regression coverage around call failure messages, failed call surfaces, compact video dock behavior, terminal write ordering, already-terminal cleanup, state-before-cleanup, bounded cleanup, file-transfer guard recovery after failed calls, and session-owned Firebase presence.

Use [AUTHENTICATION_AUDIT.md](../../AUTHENTICATION_AUDIT.md) and [ROOT_AUTH_STARTUP_REMEDIATION_ROADMAP.md](../../ROOT_AUTH_STARTUP_REMEDIATION_ROADMAP.md) as the current evidence lock for logout/account lifecycle/splash/navigation failures. Phase 1 through Phase 6 have reduced stale cached identity, logout/reset, split startup-readiness, route-local splash risk, protected-route readiness risk, and account-scoped provider lifecycle leakage. 2026-06-04 account deletion is implemented and release-gate proven: Settings prompts for confirmation and password reauth; backend deletion tombstones identity and deletes Firebase Auth last; bad-password reauth preserves the active session; successful/post-tombstone destructive deletion clears local session; login/upsert/search writes cannot recreate missing or tombstoned backend accounts after Auth succeeds. 2026-06-07 hardening adds the missing safety boundary: required tombstone failure preserves the signed-in session instead of acting like logout, optional RTDB cleanup no longer shares the tombstone's all-or-nothing write, delete failures use a modal error, and provider state no longer switches global startup to loading while password/tombstone preflight is still non-destructive. Auth lifecycle release-gate integration and Firebase emulator account-deletion proof passed in `Build Rain Apps` run 26957834309 at `883886a`.

Auth Phase 1 progress 2026-06-03: cached Drift identity is now backend-validated before signed-in restoration. Deleted backend accounts and uid mismatches clear local session, and login/register save local identity only after backend identity/presence writes. Auth Phase 2 progress 2026-06-03: logout clears local session before best-effort backend sign-out, including failed sign-out and logout-after-app-exit shutdown cases. Auth Phase 3 progress 2026-06-03: `AppStartupState` centralizes update/session/runtime/router readiness and tests cover every startup phase. Auth Phase 4 progress 2026-06-03: `RainApp` globally replaces routed content with `RainStartupSurface` during blocked startup phases, and tests prove the navigation shell is absent. Auth Phase 5 progress 2026-06-03: protected routes use explicit readiness gates and signed-out auth renders outside the shell. Auth Phase 6 progress 2026-06-03: `AuthenticatedSession.sessionGeneration` scopes runtime/provider ownership and tests prove account state resets at session boundaries.

Auth account deletion progress 2026-06-04: `SignalingAdapter` and `FirebaseSignalingAdapter` now expose reauthentication and deletion, Settings exposes the delete flow, runtime/provider teardown handles destructive and non-destructive failures separately, login refuses missing/tombstoned backend identity after Auth succeeds, and local targeted plus full Melos validation passed.

Auth teardown follow-up 2026-06-06/07: `rain-diagnostics-2026-06-06T170224-576454Z.json` showed logout/delete leaving Settings inside a startup surface while bottom navigation remained usable, and emulator follow-up showed logout could stick on loading. Runtime-provider logout now clears local session data first, detaches protected-session authority, and renders signed-out while cleanup continues best-effort. Destructive account deletion now reauthenticates and invokes backend/Auth deletion before runtime/local teardown. Successful deletion or post-tombstone destructive failure clears local session; bad-password and required tombstone failure restore the active session and show a modal error. Optional RTDB account cleanup is best-effort after tombstone so a denied mirror cannot return the user to Settings with no result. `RuntimeController.deleteAccount` keeps app startup ready while password/tombstone preflight is pending, so Settings remains mounted for wrong-password feedback; after password verification succeeds, the app enters a full-screen `deletingAccount` overlay with navigation hidden while preserving the route for backend-failure recovery. Successful deletion records a same-device deleted-username marker so a later login can show deleted-account copy without making tombstones publicly readable. The post-delete Android log showed stale account RTDB listeners receiving permission denied after Auth sign-out; the latest boundary fix cancels those listeners after backend tombstone/optional cleanup and before Firebase Auth deletion. The latest live backend follow-up hardened and deployed/read back the `users/{username}` rule so legacy rows missing `uid` can be tombstoned only by the matching Firebase Auth email/current uid. `AppStartupState` blocks protected shell rendering for runtime loading/null runtime, and runtime connect actions reject after shutdown starts. The next proof is retrying the reported Android account against the deployed rules and listener-boundary build; device smoke can be added if this user-facing flow becomes a release-gate scenario.

Diagnostics export follow-up 2026-06-06: Android returned a newline-split picker handle like `/\ndocument/11`, which bypassed the earlier `/document/...` SAF guard and was opened as a `dart:io` file path. `CrashDiagnosticsService` now normalizes leading separators/whitespace before detecting `document/` and `tree/` handles, so it writes the same fallback JSON copy instead of throwing `PathNotFoundException`.

Diagnostics export follow-up 2026-06-07: Android `file_picker 12.0.0-beta.3` can return a SAF path like `/document/12` and then the plugin's Dart wrapper attempts a second write through `dart:io`, throwing before Rain's post-picker fallback runs. `CrashDiagnosticsService` now uses the plugin method channel directly on Android with `bytes` included, treats returned SAF/content handles as platform-managed documents, and falls back to an app-owned export if a legacy wrapper failure still appears.

Registration conflict progress 2026-06-04: live Firebase rules allowed fresh random account creation, while the reported `eslam` username already existed in RTDB. Registration now converts RTDB permission-denied on account-data creation into a friendly username/account conflict, avoids caching local identity on backend-save failure, and limits Auth rollback to failures before the durable user row exists.

Terminal cleanup progress 2026-06-04: `rain-diagnostics-2026-06-04T144952-237539Z.json` showed mobile-to-PC media reached `connected`, Firebase ended as `busy`, local call cleanup stalled, file transfer was blocked by stale active-call state, and Windows shutdown did not complete. Runtime now publishes terminal state before bounded cleanup, and desktop close has bounded close/destroy plus a Windows exit fallback. The next proof is real Android-to-Windows smoke on a fresh build.

Splash Phase E progress 2026-06-04: Android light and night `LaunchTheme` plus `NormalTheme` now all use the dark `@drawable/launch_background`, and a resource contract test locks the `#061017` launch drawable so the native window does not flash white before Flutter renders the splash.

Update warning progress 2026-06-04/06: the missing warning was traced to release metadata equality and then live policy drift. Installed `1.0.6+7` apps saw a `1.0.6+7` Remote Config policy, so they correctly reported `current`; later diagnostics showed `1.0.7+8` apps reporting `remotePolicyOutdated` because live Remote Config still served `1.0.6+7`. After `rain-test-117-1`, `1.0.7+8` installs still reported `current` because the new test artifact did not bump app metadata. The app and release manifests are now bumped to `1.0.8+9`, `version_metadata_test.dart` proves checked-in policy behavior for previous `1.0.6+7` and `1.0.7+8` installs, `Build Rain Apps` run 27062729519 published `rain-test-118-1`, and live `rain-8fb4b` Remote Config version 9 now advertises `1.0.8+9`.

Senior audit Phase 3 progress 2026-06-06/09: the combined call runtime/UI truth slice is locally implemented and release-workflow proven at `Build Rain Apps` run 27051067657 for SHA `71f02e5b16614fe4c8b40fb9b6ba32ebf8f009cb`. `CallErrorClassifier` owns call failure reason/message/taxonomy/retry classification; `call_media_session_coordinator.dart` owns app-side audio/video media adapters, media diagnostics mapping, and renderer target classification; `VoiceCallTerminalReconciler` owns the pure late-session-state decision after terminal failure; `ConnectionDiagnostics` plus `peerConnectionDiagnosticsProvider` own peer UI status projection for stale data lane, failed call, recovering call, out-of-sync, manual disconnect, connected, ready, and offline states. 2026-06-08 Phase 3a grouped extracted call coordinators under `apps/rain/lib/application/runtime/voice_call/` and added `VoiceCallStateCoordinator` for pure start-block expiry, protocol session phase/detail/failure mapping, remote media permission mapping, terminal-write failure state, same-live-session guards, and local-end reset policy. 2026-06-08 Phase 3b added `VoiceCallPreflightCoordinator` for call-start availability/friend/presence guards plus stale retry replacement, and `VoiceCallReconnectCoordinator` for reconnecting/failure state plus reconnect grace timers. 2026-06-08 Phase 3c added `VoiceCallMediaCoordinator`, `VoiceCallSessionStateCoordinator`, and `VoiceCallSignalingCleanupCoordinator` for app-side media lifecycle, session-state projection/diagnostics, Firebase room/frame/ICE handling, terminal writes, stale cleanup, and bounded cleanup. 2026-06-09 Phase 4 converted voice-call, connection-request, file-transfer, and friend runtime extension files from shared `part of` files to imported/exported Dart libraries. Focused projection, chat stale-presence, terminal reconciler, local renderer failure, remote renderer failure, split-state diagnostic, state/room/terminal coordinator, reconnect/preflight coordinator, diagnostics-contract/media-path ownership, app analyze, and full Melos tests passed. Phase 3 must continue with lock/lease coordination, command coordination, full conflict policy ownership, and device-direction proof before SAR-001 can be called fully mitigated.

Direct-connect ICE diagnostics progress 2026-06-06: `rain-diagnostics-2026-06-06T034623-022529Z.json` showed `signaling.writeICE` permission denied. The fix should not broaden RTDB ICE bucket authority; current rules intentionally keep `callerICE` owned by `userA` and `calleeICE` owned by `userB`. The hardening ignores queued ICE callbacks after disconnect/session replacement and preserves sanitized path-template evidence in `lastCrash.context`. Local validation passed, and `Build Rain Apps` run 27052313608 published `rain-test-116-1` for SHA `ff4e10b7b89533a4bdbdac5cb3d412795a9e6a82`. The later `rain-diagnostics-2026-06-06T170224-576454Z.json` exposed a valid late connected-session ICE write denied because the active room had been deleted at connected; Rain now keeps the connected room alive until session cleanup, with local protocol regression coverage.

Senior audit Phase 4 progress 2026-06-05: diagnostics export privacy is locally mitigated for covered samples. `DiagnosticsSanitizer` recursively redacts/pseudonymizes crash diagnostics, debug logs, coalesced event records, write-failure debug output, and final export payloads. `CallErrorClassifier.failureTaxonomy` now separates Firebase permission denial, media permission denial, terminal rooms, stale lock repair, malformed remote data, ICE failure, and TURN unavailability. Focused diagnostics/debug-log/classifier tests passed locally. New private diagnostics fields still need sanitizer regression samples.

Diagnostics follow-up 2026-06-06: `rain-diagnostics-2026-06-06T115134-870017Z.json` showed live Remote Config is now current for `1.0.7+8` and no longer shows the old direct ICE permission crash. The active call failure was a `ringing -> failed` terminal room with reason `Busy.`, which is a callee busy response unless the remote peer export proves a stale lock. The export also exposed that recursive sanitization truncated the top-level raw `events` array to 20 heartbeat/UI records while summaries were built from 200 records. Local diagnostics now preserve the 200-event export window and split `peer_busy_response` from `real_busy_lock`.

Android build-log follow-up 2026-06-06: Rain's app module no longer applies the Kotlin Gradle Plugin, and `flutter build apk --debug` passes with `MainActivity` as Java. Remaining Kotlin Gradle Plugin warnings are from third-party plugin Android modules and should be handled by dependency upgrades when compatible releases exist. The Firebase Remote Config ABT/Analytics warning is non-blocking for update checks unless Rain explicitly adopts Analytics/A-B Testing.

Senior audit Phase 5 progress 2026-06-05: local data security is now an explicit accepted-scope decision in [[ADR-010]]. Rain currently stores message content, queued outgoing message content, file-transfer names, and local file paths in plaintext Drift/SQLite. This closes SAR-005 as documented acceptance only; it does not implement encryption. Release, support, and privacy copy must not claim local database encryption unless a future encrypted-storage phase lands with key management and migration proof.

Senior audit Phase 7 progress 2026-06-05: file-transfer streaming and backpressure are locally mitigated. Incoming transfers now keep a persistent per-transfer temp-file sink, terminal cleanup closes sinks and deletes temp files, outgoing sends carry one partial chunk instead of front-trimming a growable list, and the file-transfer protocol owns chunk/high/low/poll/timeout constants. Focused local tests passed for large receive, scripted slow receiver/backpressure, cancel cleanup, hash mismatch cleanup, disk write failure, and protocol constants. The remaining file-transfer confidence item is real-network/device-scale large-transfer proof.

Senior audit Phase 8 progress 2026-06-05: release gate unification is locally mitigated. Direct tag-push production publishing is disabled; `Release Rain` validates before build/publish and requires Remote Config deploy/readback evidence; `Build Rain Apps`, `Fast Release Apps`, `Validated Release Apps`, and `Release Rain` attach `rain-release-metadata.json`; `rain-test-*` release notes label artifacts as `TEST ARTIFACT ONLY`; production fast/validated/stable publishing requires Remote Config evidence. The next proof is a fresh GitHub Actions run for the changed workflow plus live Remote Config readback before promoting any specific artifact.

Senior audit Phase 9 progress 2026-06-05: Obsidian semantic enforcement is locally mitigated. `scripts/check_obsidian_vault.ps1` now validates operational register schemas across [[Audit Resolution Tracker]], [[Technical Debt Register]], [[Risk Register]], [[BLOCKERS]], [[Project Metrics]], [[Recommended Next Actions]], and [[Repository Map]]. The validator fails missing owner/priority/evidence/review fields, unsupported active-risk/debt status values, closed blockers without evidence, evidence-ledger gaps, and P0/P1 items without a next-action-equivalent field.

Senior audit Phase 10 progress 2026-06-05: device/media proof is now scoped and executable, but not passed. `apps/rain/integration_test/device_media_reality_proof_test.dart` uses the real `FlutterWebRTCBridge` and `DefaultCallMediaConnection` to require microphone and camera tracks when run with `--dart-define=RAIN_DEVICE_MEDIA_PROOF=true`. Android QA tooling is installed and `QA_Medium_API_36_1` exists, but no Android device/emulator was attached (`flutter devices` showed Windows/web only, `adb devices` was empty). Existing saved Rain Appium artifacts timed out in WebDriver and cover only auth toggle smoke, not calls or media. Phase 10 remains release-blocking until the target device matrix is actually run or the launch scope is explicitly reduced.

## Top Recommended Actions

| Rank | Action | Why Now | Dependencies | Success Criteria |
| --- | --- | --- | --- | --- |
| 1 | Retry Android account deletion with the listener-boundary build. | The app now preserves the session on recoverable failures, blocks the whole app during verified destructive deletion, cancels account RTDB listeners and active protocol sessions before Auth deletion, and live rules were deployed/read back to address the missing-uid denial path. | [[Authentication]], [[Rules Strategy]], BLK-010, BLK-003 | Wrong password stays in the Settings modal, verified deletion shows full-screen "Deleting account" with no navigation, successful delete signs out without stale RTDB/data-room permission-denied listener errors, and retrying the deleted username shows deleted-account copy or a different specific backend/Auth boundary. |
| 2 | Verify installed-app update behavior against live Remote Config version 9. | `1.0.8+9` artifacts and live policy now exist; the remaining proof is that real installed clients fetch and render the correct required/current states. | [[Version And Updates]], [[Release Gates]], BLK-004, BLK-006 | Old `1.0.7+8` installs show required update, current `1.0.8+9` installs report current, and diagnostics/settings evidence records channel/platform/build. |
| 3 | Run Phase 10 device/media proof, starting with the opt-in media capture test and Android-to-Windows SCN-CALL-004. | Firebase call lock/rule proof passes locally and Phase 3 now proves renderer failure/false UI split locally, but no attached Android endpoint was available in the 2026-06-05 session. | [[Scenario Coverage Matrix]], [[Emulator Test Matrix]], BLK-001, BLK-007 | `device_media_reality_proof_test.dart` passes on an attached device/emulator with mic/camera, then mobile-to-PC voice/video call evidence proves no stale unavailable session state, file transfer is allowed after terminal call state, Windows closes cleanly, and diagnostics/artifacts are recorded. |
| 4 | Continue Phase 3 voice call runtime decomposition. | Projection, renderer authority, classifier, media-adapter, terminal-session policy, room helper policy, pure state mapping, call-start preflight, reconnect grace, media lifecycle, session-state projection, signaling-cleanup, and runtime import/export slices are proven, but `VoiceCallRuntime` still owns command handling, lock/lease orchestration, full call/file conflict policy, and some local end-state sequencing. | TASK-003, [[Call State Machine]], [[CallTerminalReconciler]], [[VoiceCallRuntime Refactor]] | Add characterization tests for command and lease paths, then extract command orchestration and lock/lease ownership with no public `RainRuntimeController` behavior change. |
| 5 | Extend diagnostics taxonomy into ICE route and media lifecycle proof. | Phase 4 sanitizer/export proof is done, but failed call exports still need selected route, candidate count, track, renderer, first-frame, and cleanup-lifecycle evidence. | [[CallDiagnosticsRecorder]], [[Diagnostics Sanitization]], TD-016 | Failed calls export categorized ICE/media lifecycle evidence without raw SDP or candidate strings, and tests prove the export remains sanitized. |
| 6 | Add Firebase emulator proof for registration conflict cleanup if adapter seams allow it. | Account-deletion tombstone/no-recreate proof now exists in the hard gate, but registration conflict partial-write cases still need stronger release proof. | [[Authentication]], [[Rules Strategy]], TD-021 | Existing username, pre-user-row denial, and post-user-row secondary failure behavior are covered without requiring live Firebase probes. |
| 7 | Add Firebase emulator proof for online receiver request denial and quota non-consumption. | Scenario coverage still has a partially covered connection-request quota path that can burn quota or notify incorrectly if presence is stale. | [[Scenario Coverage Matrix]], [[Emulator Test Matrix]], BLK-009, BLK-003 | Online receiver denial is proven at runtime/rules level and request quota remains unchanged. |
| 8 | Run real-network/device-scale large file transfer proof. | Phase 7 local runtime proof is complete, but release-scale confidence still needs real peer/device behavior under slow receiver or poor network conditions. | [[Streaming Architecture]], [[Backpressure Strategy]], R-011, TD-008 | A large Android-to-Windows or Windows-to-Android transfer completes or fails cleanly with bounded diagnostics, no stale temp files, and no uncontrolled channel buffering. |
| 9 | Close the Gap rows in [[Scenario Coverage Matrix]]. | The first applied pass found concrete gaps around newer live call locks and device/media lifecycle proof; recursive diagnostics sanitization now has focused local proof but future field changes still need samples. | [[Scenario Coverage Matrix]], [[Emulator Test Matrix]], [[Diagnostics Sanitization]] | Gap rows are either covered by named tests or explicitly accepted with owner rationale. |
| 10 | Add generated metric reconciliation to the vault validator. | Phase 9 validates register semantics, but counts such as open blockers, P0 debt, and critical risks are still manually maintained. | [[Project Metrics]], [[Risk Register]], [[Technical Debt Register]], [[BLOCKERS]] | Validator compares metric counts against parsed registers or clearly marks metrics as manually reviewed with owner/date/evidence. |
| 11 | Add traceId coverage to heartbeat, presence, signaling writes, and file transfer (Phase 12 of [[Master Roadmap]] tracing overlay). | R-023 and TD-024 document that the 2026-08-29 slice only spans `auth/*` and `call/*`; cross-flow queries on `presence/*`, `operation_*`, and `ui_state` events still cannot be tied to one user action. | [[Diagnostics And Logging]], [[ADR-011]], [[Peer Chat]], [[Call State Machine]], [[File Transfer]] | Each affected call site wraps in `TraceContext.runAsync`; regression tests assert `traceId` propagation through one full heartbeat cycle, one presence flip, and one file transfer round-trip. |
| 12 | Add structural `==`/`hashCode` to `PeerConnectivitySnapshot` and `ConnectionDiagnostics` (Phase 13 of tracing overlay). | `ThrottledProviderObserver` currently dedupes on `Object.hashCode`; identity-equal updates can still emit, and `hashCode` collisions across distinct values can silently drop legitimate updates. | [[Diagnostics And Logging]], [[Call State Machine]], [[Peer Chat]] | The two value types override `==`/`hashCode` on every diagnostic-relevant field; regression tests prove identity-equal updates are dropped and distinct values are not. |
| 13 | Add Drift event persistence and a debug overlay UI (Phase 14 of tracing overlay). | Current slice is in-memory only; persistent cross-session trace queries and a live tail are deferred work tracked in the original 2026-08-27 design. | [[Database Architecture]], [[Diagnostics And Logging]], [[Diagnostics Sanitization]] | `app_events` table indexed on `(traceId, recordedAt)` with 7-day TTL; `/debug/traces` page; `DiagnosticsSheet` floating widget gated behind `kDebugMode` or `updateChannel == 'demo'`. Sanitizer regression samples for every new persisted field. |

## Do Not Prioritize Yet

- Broad visual redesign before call state ownership is stable.
- Making Appium a hard gate before the smoke flow is repeatable.
- Adding new Firebase paid services because Spark/free-tier remains a hard constraint.
- Implementing local database encryption as a drive-by change. It needs a separate Option B plan covering key storage, migration, rollback, recovery, and tests.
- Deleting old runtime paths before characterization tests exist.

## Next-Action Generation Rule

After every completed task:

1. Update [[Lessons Learned]].
2. Update [[Project Metrics]] if counts or readiness changed.
3. Update [[Improvement Backlog]] if a recurring pattern appears.
4. Update this note with the next three most valuable actions.
5. Update [[Project Memory]] only when durable project facts change.

Cross-platform remediation progress 2026-08-26: [CROSS_PLATFORM_REMEDIATION_PLAN.md](../../CROSS_PLATFORM_REMEDIATION_PLAN.md) was created from the 2026-08-26 cross-platform audit, and the first execution slice landed. A1 moved receiver final SHA-256 into Isolate.run; A2 caches incoming transfer records across chunks with clearTransferRuntimeState as the single invalidation funnel, removing the per-chunk loadById hot-loop read; a loadById-count regression test locks the behavior. Two pre-existing HEAD blockers were repaired en route: 254 missing library; directives that failed the CI --fatal-infos analyzer gate (commit 595a0fe), and _runtimeProviderContainer omitting a non-demo signaling key so 7 account-deletion/logout tests failed the hard cipher check (commit 30c46cc). Workspace analyze and full Melos tests passed. Next recommended slice: A3+A4 - filter binary frames out of the connectivity fan-out and throttle receive sink flushes - followed by A5 narrow HomeScreen watches.

Cross-platform remediation progress 2026-08-26 (slice 2): A3 stopped per-chunk peer-snapshot rebuilds - binary-only frames are filtered from the provider subscription and runtime data-event notifications use a leading-edge 250 ms throttle with trailing flush (data_event_throttle_test.dart locks emission counts). A4 replaced flush-per-chunk with FileTransferFlushPolicy in rain_core: first write flushes immediately to preserve fail-fast disk-error semantics, later writes batch per 512 KiB, terminal closes cover the remainder. Workspace analyze and full Melos tests passed. Next recommended slice: A5 - narrow HomeScreen provider watches with .select() projections and a rebuild-counter widget test.

Tracing overlay progress 2026-08-29: PR1+PR3 of `docs/plans/2026-08-27-rain-tracing-implementation-guide.md` are wired into `apps/rain/lib/infrastructure/diagnostics/tracing/` per [[ADR-011]]. `IdentityController.register` and `RainRuntimeController._startCall` share one `traceId` for the entire flow; `AppNavigationObserver` is registered on the `GoRouter` observers list; `ThrottledProviderObserver` replaces `RainDebugProviderObserver`. Workspace `dart run melos run analyze` and `dart run melos run test` both SUCCESS across all 4 packages (`peer_core` 75, `protocol_brain` 267, `rain` 741 + 20 skipped, `rain_core` 51; 1134 passed, 0 failed). Residual gap: traceId is not yet on heartbeat, presence, signaling writes, file transfer, or `createOutgoingCall`. The throttling observer still uses non-structural `hashCode` and a hardcoded `runtimeType` key. [[Risk Register|R-023]] and [[Technical Debt Register|TD-024]] track the remaining scope. Next tracing slices: heartbeat + presence + signaling writes (Phase 12), structural `==`/`hashCode` on noisy providers (Phase 13), Drift event persistence + `/debug/traces` overlay (Phase 14).
