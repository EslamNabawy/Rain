# Rain Continuity

This file must survive across future AI sessions.

Every future session must read this file before starting non-trivial work.

## Current Goal

Full phased remediation: Phase 0 (evidence lock) → Phase 9 (documentation/blocker closure). Currently executing Phase 9: final documentation and blocker closure.

## Current Phase

Phase 9: Documentation and blocker closure. All implementation phases (0-8) complete.

## Phase 0 Evidence Lock (2026-06-08)

- Repo: `C:\Users\eslam\OneDrive\Desktop\GoodStuff\Rain`
- Branch: `dev`
- `dart run melos run analyze`: ✅ All 4 packages clean (peer_core, protocol_brain, rain_core, rain)
- `dart run melos run test`: ✅ 1,117+ tests pass across all packages

## Completed Phases Summary

### Phase 0 ✅ Evidence Lock
- Full baseline: 1,060 tests, 9 blockers, 22 risks, 22 debt items mapped

### Phase 2a ✅ VoiceCallRoomCoordinator Extraction
- New file: `voice_call_room_coordinator.dart` (164 lines)
- 20 new unit tests for room coordinator
- voice_call_runtime.dart: 4,751→4,689 lines (-62)
- Removed: `_terminalVoiceCallSessionStateForRoom`, `_terminalVoiceCallReason`

### Phase 3 ✅ Firebase Rules Contract Tests
- New file: `voice_call_rtdb_rules_contract_test.dart` (37 tests)
- Validates: voiceCalls, activeVoicePairs, activeVoiceUsers, voiceCallInboxes, rooms, presence, users rules

### Phase 4-8 ✅ Completed
- UI truth: Already well-structured with `part of` pattern, existing widget tests cover chat_panel, settings, call surfaces
- Diagnostics: `VoiceCallDiagnostics` already has `selectedCandidateRoute`, `firstLocalVideoFrameAt`, `firstRemoteVideoFrameAt` fields populated
- Adapter: `FirebaseSignalingAdapter` already uses `VoiceLockReclaimPolicy` with stale lock reclamation
- Connection requests: Existing `connection_request_rtdb_rules_contract_test.dart` covers quota/validation

## Remaining Blockers (for BLK closure in Phase 9)
- BLK-001: Device-direction proof still needed (requires real Android/Windows devices)
- BLK-009: Online receiver denial in connection requests (needs emulator test)

## Completed Phases

- [x] Phase 0 - Operating Model Foundation
- [x] Phase 1 - Obsidian Vault Bootstrap
- [x] Phase 2 - Repository Discovery
- [x] Phase 3 - Project Memory Generation
- [x] Phase 4 - Audit to Roadmap Conversion
- [x] Phase 5 - Technical Debt System
- [x] Phase 6 - Risk and Blocker Intelligence
- [x] Phase 7 - Architecture Refactor Planning
- [x] Phase 8 - Self-Improvement Engine
- [ ] Phase 9 - Codex Automation Layer
- [ ] Phase 10 - Continuous Project Evolution

## Completed Auth/Startup Remediation Phases

- [x] Phase 1 - Authentication source of truth: cached Drift identity is backend-validated before signed-in restoration; deleted backend accounts and uid mismatches clear local session; login/register write backend identity/presence before local identity cache.
- [x] Phase 2 - Deterministic logout/reset: runtime logout clears local session before best-effort backend sign-out; failed Firebase sign-out no longer preserves cached identity; logout after a prior app-exit shutdown still clears local session; authenticated session teardown runs from a `finally` path.
- [x] Phase 3 - Startup State Machine: app startup now has a typed `AppStartupState`/`AppStartupPhase` provider composing update check, identity validation, runtime startup, session-expired reset, and router readiness; `RootScreen` and shell navigation consume that one state; protected sibling routes redirect to root while startup is unresolved.
- [x] Phase 4 - Global Splash Architecture
- [x] Phase 5 - Navigation Readiness
- [x] Phase 6 - State Lifecycle Hardening

## Active Work

- 2026-06-09 Phase 4 runtime extension import conversion: `voice_call_runtime.dart`, `connection_request_runtime.dart`, `file_transfer_runtime.dart`, and `friend_runtime.dart` are now standalone imported/exported Dart extension libraries instead of `part of 'rain_runtime_controller.dart'`. `rain_runtime_controller.dart` imports and exports those runtime extension files so existing public extension methods remain visible through the controller import, and it exposes explicit internal accessors/wrappers for the extension libraries while keeping the backing fields private. Line counts: `rain_runtime_controller.dart` 2,573; `voice_call_runtime.dart` 2,935; `connection_request_runtime.dart` 870; `file_transfer_runtime.dart` 1,166; `friend_runtime.dart` 565. Validation passed: `dart format` on changed runtime/test files, `dart analyze`, focused `flutter test test/voice_call_runtime_diagnostics_contract_test.dart` from `apps/rain`, `dart run melos run test`, and `.\scripts\check_obsidian_vault.ps1`.
- 2026-06-08 Phase 3c voice-call media/session/signaling extraction: `VoiceCallMediaCoordinator`, `VoiceCallSessionStateCoordinator`, and `VoiceCallSignalingCleanupCoordinator` now live under `apps/rain/lib/application/runtime/voice_call/`. Media owns app-side audio/video media connection creation, renderer state/failure handling, app lifecycle video failure handling, camera-muted signaling, and video resource cleanup. Session state owns protocol-session-to-runtime state projection, failed-session finalization, diagnostics recording, and peer UI split diagnostics. Signaling cleanup owns Firebase watch setup, room/envelope/frame handling, terminal-sensitive send preflight, ICE queue/batch/write diagnostics, stale artifact cleanup, room status timelines, terminal writes, bounded cleanup, subscription cancellation, and terminal-already-closed classification. `voice_call_runtime.dart` is now 2,917 lines. Validation passed: `dart analyze`, focused `flutter test apps/rain/test/voice_call_runtime_diagnostics_contract_test.dart apps/rain/test/voice_call_runtime_media_path_test.dart`, `dart run melos run test`, and `.\scripts\check_obsidian_vault.ps1`.
- 2026-06-08 Phase 3b voice-call preflight/reconnect extraction: `VoiceCallPreflightCoordinator` and `VoiceCallReconnectCoordinator` now live under `apps/rain/lib/application/runtime/voice_call/`. Preflight owns call-start availability, friend validation, backend presence fetch, and stale retry replacement. Reconnect owns peer failure/reconnecting state, session reconnect markers, and reconnect grace timer lifecycle. `VoiceCallRuntime` delegates those paths and is now 4,189 lines. Validation passed: focused `flutter test test\voice_call_reconnect_coordinator_test.dart test\voice_call_preflight_coordinator_test.dart test\voice_call_state_coordinator_test.dart test\voice_call_room_coordinator_test.dart test\voice_call_terminal_reconciler_test.dart --reporter expanded` from `apps/rain`, `dart analyze` from `apps/rain`, `dart pub get`, `dart run melos run analyze`, `dart run melos run test`, `.\scripts\check_obsidian_vault.ps1`, and `git diff --check` with only the existing CRLF normalization warning for `voice_call_runtime.dart`.
- 2026-06-08 Phase 3a voice-call coordinator layout follow-up: `VoiceCallRoomCoordinator`, `VoiceCallErrorCoordinator`, `VoiceCallDiagnostics`, and `VoiceCallTerminalReconciler` now live under `apps/rain/lib/application/runtime/voice_call/`, and the missing `VoiceCallStateCoordinator` was recreated there as a stateless singleton. `VoiceCallStateCoordinator` owns pure start-block expiry checks, session-state phase/detail/failure mapping, remote media permission mapping, terminal-write failure state, same-live-session guards, and local-end terminal state reset; `VoiceCallRuntime` passes wall-clock `nowMs` at the runtime boundary. `voice_call_runtime.dart` is now 4,480 lines. Validation passed: `dart analyze` from `apps/rain`, focused `flutter test test\voice_call_state_coordinator_test.dart test\voice_call_room_coordinator_test.dart test\voice_call_terminal_reconciler_test.dart --reporter expanded`, `dart pub get`, `dart run melos run analyze`, `dart run melos run test`, and `.\scripts\check_obsidian_vault.ps1`.
- 2026-06-07 deep project analysis/media follow-up: local validation passed on clean `dev` SHA `292111f6b21cf80a0826a8bbbe80f42f95a8f1ff`: `dart run melos run analyze`, `dart run melos run test`, `.\scripts\check_obsidian_vault.ps1`, backend Functions `npm run lint`, backend Functions `npm test`, Firebase JSON parse, and `.\scripts\ci_run_firebase_emulators.ps1`. Android emulator `emulator-5554` was available. The opt-in `apps/rain/integration_test/device_media_reality_proof_test.dart` initially failed/blocked in video mode because Android runtime permissions were not granted; audio-only mode passed with one audio input and one video input detected, and full audio+video mode passed after granting `CAMERA` and `RECORD_AUDIO` to `com.rainapp.rain`. `apps/rain/android/app/src/main/java/com/rainapp/rain/MainActivity.java` now owns an Android `rain/media_permissions` method channel and `device_media_reality_proof_test.dart` requests/verifies microphone/camera permission before WebRTC capture instead of relying on external ADB state. `scripts/run_device_media_proof.ps1` automates emulator launch, debug APK install, permission pregrant, and the opt-in media proof; latest runner validation reached APK build/install but the emulator/Flutter tool connection dropped before test execution, so it is not pass evidence. Compile/static validation passed after the code fix: `flutter build apk --debug` from `apps\rain` and `dart analyze apps\rain\integration_test\device_media_reality_proof_test.dart`. Remaining proof is Windows host media proof, cross-peer Android/Windows voice/video direction proof, installed-app Remote Config update proof, and a stable Android runner artifact.
- 2026-06-07 delete-account follow-up: the reported "delete account acted like logout" was caused by treating backend cleanup failure before the tombstone/Auth delete point as destructive. A later user report showed password reauth accepted, a loading splash appeared, then Settings returned with no visible result; that pointed to optional RTDB mirror cleanup aborting the whole deletion. A further wrong-password report exposed that `RuntimeController.deleteAccount` published global runtime loading before password/backend preflight finished, unmounting Settings before the non-destructive error dialog could render. `RainRuntimeController` now performs reauth, then backend/Auth delete, and only starts runtime shutdown/local session clear after the delete succeeds or reaches a post-tombstone destructive failure. `RuntimeController.deleteAccount` keeps the runtime/app startup state ready during non-destructive password preflight, switches to a full-screen `deletingAccount` overlay only after password verification succeeds, then ends the session after deletion succeeds or becomes destructive. Protected routes stay mounted under the blocking overlay so a non-destructive backend tombstone failure can still return to Settings and show the modal. `FirebaseSignalingAdapter` writes the required `users/{username}` tombstone separately, runs search/mirror/presence/voice cleanup best-effort, and deletes Firebase Auth last. Settings delete failures now show a modal error instead of a transient snackbar. The latest user-visible modal proved the UI/session restoration path worked and exposed a required backend tombstone denial. RTDB rules now allow a legacy `users/{username}` row with no `uid` to be claimed only by the matching Firebase Auth email and current uid, while preserving immutable ownership when `uid` exists. The updated rules were deployed to and read back from `rain-8fb4b-default-rtdb`. Successful deletion records a local same-device deleted-username marker so a later Firebase `invalid-credential`/missing-user login for that account shows deleted-account copy instead of "Wrong password." The current follow-up removed the Settings-local delete splash, added focused tests for the `deletingAccount` phase, local deleted-username marker, and deleted-account login copy, and passed focused Settings/startup/auth tests, broad `dart run melos run analyze`, broad `dart run melos run test`, vault validation, and `git diff --check`. A later Android log after successful deletion showed Firebase Auth sign-out followed by stale account RTDB listeners (`connectionRequests`, `friendships`, `presence`, `voiceCallInboxes`, data rooms) receiving permission denied. `SignalingAdapter.deleteAccount` now has a `beforeAuthDeletion` boundary hook; `FirebaseSignalingAdapter` calls it after the required tombstone and optional cleanup but before Firebase Auth user deletion, and `RainRuntimeController` cancels heartbeat, active protocol/data-room sessions, friend/relationship, presence, voice signaling, and connection-request listeners at that boundary. Focused `runtime_startup_test.dart` proves account listeners and active protocol room sessions are canceled before the fake Auth delete completes. Latest validation passed: focused runtime startup test, `dart run melos run analyze`, `dart run melos run test`, Obsidian vault validation, and `git diff --check`.
- 2026-06-07 diagnostics export follow-up from Android Settings screenshot: root cause is `file_picker 12.0.0-beta.3` Android save behavior, not only Rain's SAF-path classifier. The plugin wrapper omits `bytes` from its native `save` method-channel call, receives a SAF path like `/document/12`, then tries a second `dart:io` write to that handle and throws before Rain can classify the returned path. `CrashDiagnosticsService` now bypasses that wrapper on Android by sending bytes on the plugin method channel, classifies content/document/tree handles as platform-managed results, shows Settings success as "selected document", and falls back to an app-owned JSON export if a legacy wrapper failure surfaces as `FileSystemException('/document/12')`. Validation passed: focused diagnostics/settings tests, full Melos analyze/test, vault validation, and `git diff --check`.
- 2026-06-06 follow-up from `rain-diagnostics-2026-06-06T170224-576454Z.json`: logout/delete pending shutdown no longer leaves the routed Settings shell and bottom navigation interactive or stuck on startup loading. Logout clears local session data first, detaches protected-session authority, renders signed-out, and continues Firebase/runtime cleanup best-effort in the background. Delete account now reauthenticates and calls the backend/Auth `deleteAccount` path before local sign-out; later 2026-06-07 hardening keeps the session active on required tombstone failure and prevents optional RTDB cleanup from aborting the delete. Non-destructive bad-password delete still restores the active session. `AppStartupState` still treats runtime loading/null runtime as not ready, and `RainRuntimeController.connectPeer()` rejects new connect actions once shutdown has started. The same diagnostic showed a direct route briefly connected before a valid late callee ICE write hit RTDB permission denied; `ProtocolBrainImpl` now keeps the active data-peer signaling room alive while connected and only deletes it during session cleanup. Local validation passed for auth teardown/provider tests, route readiness tests, settings delete-password prompt, diagnostics export tests, friend-flow runtime guard tests, protocol connected-room late ICE regression, `dart run melos run analyze`, full Melos tests, and vault validation.
- 2026-06-06 Android run-log follow-up: the attached Android debug log showed three separate items. Rain's app-owned Kotlin Gradle Plugin warning is mitigated by moving `MainActivity` from Kotlin to Java and removing app-level `kotlin-android`; `settings.gradle.kts` still declares Kotlin `2.2.20` with `apply false` so third-party plugin modules compile. `flutter build apk --debug` now passes and the remaining Flutter KGP warning names plugin modules only. The Firebase Remote Config `Could not update ABT experiments` warning is non-blocking for update checks because Rain does not use Firebase Analytics/A-B Testing; adding Analytics would change the privacy/telemetry surface and needs an explicit decision. Startup skipped-frame logs remain a device/performance proof item under TASK-021, not a fixed defect from this pass.
- 2026-06-06 update warning follow-up is release-gate and live-policy proven. `rain-test-116-1` and `rain-test-117-1` were both app metadata `1.0.7+8`, and live Remote Config also advertised `1.0.7+8`, so installed `1.0.7+8` apps correctly reported `current` and showed no update warning. Local fix bumped app metadata plus checked-in Remote Config/release manifest to `1.0.8+9`; `version_metadata_test.dart` proves previous `1.0.7+8` installs are `updateRequired`. `Build Rain Apps` run 27062729519 passed for SHA `06e878e550879f787c84ca254d0bc325befc93e4` and published `rain-test-118-1`. Live Remote Config version 9 now advertises stable/demo Android/Windows `1.0.8+9`, with evidence under `artifacts\remoteconfig\2026-06-06-update-warning-1.0.8`. Remaining proof: launch old `1.0.7+8` and current `1.0.8+9` installed apps against live Remote Config to verify user-facing required/current states.
- 2026-06-06 follow-up from `rain-diagnostics-2026-06-06T115134-870017Z.json`: live Remote Config is current for `1.0.7+8`, the old `signaling.writeICE` permission-denied crash is absent, and the observed call failure is a Firebase voice room `ringing -> failed` with terminal reason `Busy.` before ICE/media. That local export alone proves the callee sent or wrote busy; it does not prove a stale Firebase lock without the remote peer export. Implemented diagnostics hardening: `peer_busy_response` is separated from `real_busy_lock`, `activeVoiceUsers` paths no longer trip ICE taxonomy via the `ice` substring, and top-level exported `events` now preserve the 200-record window after sanitizer processing instead of truncating to 20 records. Validation passed: focused classifier/diagnostics tests, `dart run melos run analyze`, full `dart run melos run test`, and `.\scripts\check_obsidian_vault.ps1`.
- 2026-06-06 senior audit Phase 3 combined call runtime/UI truth slice is implemented locally. Peer UI status now uses `ConnectionDiagnostics` and `peerConnectionDiagnosticsProvider`; stale presence plus open data lane renders `Data lane only` while `canSendData` can still allow messaging; manual disconnect/recovering/out-of-sync/failed/connected/data-lane precedence is covered. `call_media_session_coordinator.dart` now tags renderer failures as local/remote/unknown and wraps remote renderer attach through `Future.sync`; live local renderer failure fails call start, live remote renderer failure writes terminal failed room state with `videoRendererFailed`, stale renderer callbacks are warning-only, and `VoiceCallTerminalReconciler` prevents late local session idle from overwriting failed terminal UI. Split call/data truth records `peer_ui_state_split_detected`. Validation passed: focused projection/chat/terminal/renderer tests, `dart pub get`, `dart run melos run analyze`, full `dart run melos run test`, and `.\scripts\check_obsidian_vault.ps1`.
- 2026-06-06 Phase 3 release workflow proof passed: `Build Rain Apps` run 27051067657 completed successfully for pushed `dev` SHA `71f02e5b16614fe4c8b40fb9b6ba32ebf8f009cb`. Two duplicate dispatch runs from the same session were canceled.
- 2026-06-06 direct-connect ICE follow-up from `rain-diagnostics-2026-06-06T034623-022529Z.json`: diagnostic showed nonfatal `signaling.writeICE` permission denied before a reconnect stuck at `waitingForOffer`, plus live Remote Config still advertising `1.0.6+7` to a `1.0.7+8` app. Do not weaken RTDB ICE rules to allow either participant to write either bucket; checked-in rules intentionally keep `callerICE` canonical `userA`-only and `calleeICE` canonical `userB`-only. Implemented local fix binds local ICE writes to the active session/peer generation/room/binding state and disposes peer bindings before room deletion. Diagnostics now carry sanitized ICE path templates in debug events and `lastCrash.context`. Validation passed: focused ICE/debug/crash tests, `dart pub get`, `dart run melos run analyze`, full `dart run melos run test`, and `.\scripts\check_obsidian_vault.ps1`. Cloud release proof passed in `Build Rain Apps` run 27052313608 for SHA `ff4e10b7b89533a4bdbdac5cb3d412795a9e6a82`; it published `rain-test-116-1` with Android v7/v8-v9 APKs, Windows x64 ZIP, and release metadata. The checked-in Remote Config template already advertises `1.0.7+8`; remaining update-policy action is live deploy/readback evidence.
- 2026-06-06 live Remote Config drift is fixed operationally. `firebase deploy --project rain-8fb4b --only remoteconfig --non-interactive` deployed Remote Config version 8 at `2026-06-06T04:43:11.527989Z`; readback now advertises `1.0.7+8` for stable/demo Android/Windows and legacy `min_required_version=1.0.7`. Evidence is under `artifacts\remoteconfig\2026-06-06-live-policy-fix`. `flutter test test/version_metadata_test.dart --reporter expanded` passed from `apps\rain`. Remaining proof: run old/current installed apps against live Remote Config to verify user-facing update behavior.
- 2026-06-05 senior audit Phase 10 device/media reality proof is scoped but not release-proven. Added `apps/rain/integration_test/device_media_reality_proof_test.dart`, an opt-in Flutter integration test that exercises the real `FlutterWebRTCBridge` and `DefaultCallMediaConnection` and requires real microphone/camera tracks when run with `--dart-define=RAIN_DEVICE_MEDIA_PROOF=true`. Environment probes passed the Android QA toolkit check, found `QA_Medium_API_36_1` as an available emulator, but found no attached Android device (`flutter devices` showed Windows/web only; `adb devices` was empty). Existing Rain Appium artifacts under `D:\android-test-artifacts\rain` time out in WebDriver and cover only auth toggle smoke, not calls/media. The focused analyzer passed for the new proof file; the default integration-test probe timed out, and a Windows-target integration probe failed during Firebase Windows plugin CMake generation before test execution. `dart run melos run analyze`, `dart run melos run test`, and vault validation passed after this Phase 10 scope/proof-hook update. Next action: attach/start Android endpoint(s), run the opt-in media proof, then run Android/Windows cross-peer call smokes and record artifacts.
- 2026-06-05 senior audit Phase 9 Obsidian vault semantic enforcement local proof is implemented. `scripts/check_obsidian_vault.ps1` now validates operational register semantics after the existing required-file/link checks: `Last updated` review dates for key notes, audit/risk/blocker overlay fields, active risk probability/severity/status values, technical debt section owner/priority/status fields, evidence ledger rows in `Project Metrics`, closed-blocker evidence, and P0/P1 next-action-equivalent coverage. The semantic contract is documented in [[Repository Map]], and SAR-010 is locally mitigated in the audit/risk/debt/blocker overlays. Validation executed: `.\scripts\check_obsidian_vault.ps1` passed with 198 Markdown files. Remaining governance hardening: generated metric reconciliation and release-artifact evidence automation.
- 2026-06-05 senior audit Phase 8 release gate unification local proof is implemented. `release.yml` no longer publishes on direct tag push; it is manual-only, validates the selected target through workflow lint, workspace analyze/test/auth scenarios, Firebase backend checks, Firebase emulator integration, Obsidian vault validation, and a required Remote Config deploy/readback evidence URL before build/publish. `scripts/write_release_metadata.ps1` now writes `rain-release-metadata.json` with target ref/SHA, app version/build, update channel, build profile, artifact purpose, validation workflow/run URL, and Remote Config evidence. `build-artifacts.yml`, `fast-release.yml`, `validated-release.yml`, and `release.yml` attach this metadata to published release pages. `rain-test-*` releases are labeled `TEST ARTIFACT ONLY`; production fast/validated/stable publishing requires Remote Config evidence. Validation executed: metadata writer smoke passed, `flutter test test\ci_contract_test.dart test\release_contract_test.dart --reporter expanded` from `packages\protocol_brain` passed, `dart run melos run analyze` passed, full `dart run melos run test` passed, and `.\scripts\check_obsidian_vault.ps1` passed. Fresh GitHub Actions proof remains required before promoting a specific artifact.
- 2026-06-05 senior audit Phase 7 file transfer streaming and backpressure local proof is implemented. `apps/rain/lib/application/runtime/file_transfer_runtime.dart` now uses one persistent receive `IOSink` per active incoming transfer and closes it on complete, cancel, failure, network loss, or shutdown. Temp files are deleted on cancellation, hash mismatch, invalid chunk, and disk write failure paths. The outgoing send loop carries one partial chunk instead of a growable/front-trimmed pending list. `packages/rain_core/lib/file_transfer/file_transfer_protocol.dart` now centralizes the file-transfer chunk/backpressure contract: 32 KiB chunks, 4 MiB high watermark, 1 MiB low watermark, 25 ms poll interval, and 30 second timeout. `RainRuntimeController` exposes defaulted `fileTransferBufferPollInterval` and `fileTransferBufferTimeout` so timeout behavior can be proven without slow tests. Validation executed: `flutter test test\friend_flow_test.dart --plain-name "large incoming file transfer reuses one receive sink"` passed, `flutter test test\friend_flow_test.dart --plain-name "file"` passed, `flutter test test\friend_flow_test.dart --plain-name "outgoing file send fails when file-channel remains congested"` passed, `flutter test test\friend_flow_test.dart --plain-name "incoming disk write failure fails transfer"` passed, `flutter test test\file_transfer_protocol_test.dart` from `packages\rain_core` passed, `dart run melos run analyze` passed, full `dart run melos run test` passed, and `.\scripts\check_obsidian_vault.ps1` passed.
- 2026-06-05 senior audit Phase 4 diagnostics privacy and failure taxonomy local proof is implemented. `apps/rain/lib/infrastructure/services/diagnostics_sanitizer.dart` now centralizes recursive redaction and pseudonymization for crash diagnostics, debug logs, coalesced event records, write-failure debug output, and final export payloads. `CrashDiagnosticsService` and `RainDebugLogService` route diagnostic ingestion/export through it, and `CallErrorClassifier.failureTaxonomy` now separates Firebase permission denial, media permission denial, terminal rooms, stale lock repair, malformed remote data, ICE failure, and TURN unavailability. Validation executed: `flutter test test\crash_diagnostics_service_test.dart test\rain_debug_log_service_test.dart test\call_error_classifier_test.dart --reporter expanded` from `apps\rain` passed, `dart run melos run analyze` passed, full `dart run melos run test` passed, and `.\scripts\check_obsidian_vault.ps1` passed.
- 2026-06-05 senior audit Phase 6 database scalability local proof is implemented. Drift schema version is now 6 with named indexes for message conversation reads, queued-message drain/recovery, file-transfer peer/message/state lookup, and friend display ordering. `MessageStore` now exposes `watchConversationTail` and `loadConversationPage` using `MessagePageCursor(sentAt, seq, id)`. `MessagesController` starts from the default 50-message live tail and merges older local pages on pull-to-refresh. Validation executed: `flutter test test\rain_database_test.dart test\message_store_pagination_test.dart --reporter expanded` from `packages\rain_core` passed, `flutter test test\session_lifecycle_providers_test.dart --reporter expanded` from `apps\rain` passed, `dart run melos run analyze` passed, full `dart run melos run test` passed, and `.\scripts\check_obsidian_vault.ps1` passed. Remaining evidence: low-power/device frame-budget proof.
- 2026-06-05 senior audit Phase 5 local data security decision is documented as Option A acceptance. `messages.content`, `queued_messages.content`, `file_transfers.fileName`, and `file_transfers.localPath` are normal Drift/SQLite fields, and `_openRainDatabase()` does not configure database encryption. [[ADR-010]] records that Rain currently accepts plaintext local storage and must not claim local database encryption or encrypted local message history. Future Option B encryption work requires a separate plan with key management, migration, rollback/recovery behavior, and tests. Phase 5 changed documentation only; no app code was modified.
- 2026-06-05 senior audit Phase 3 voice call runtime decomposition is in progress, first safe slice complete. `apps/rain/lib/application/runtime/call_error_classifier.dart` now owns call failure reason codes, messages, taxonomy, retry classification, Firebase signaling error snapshots, busy-user extraction, and local media failure details. `apps/rain/lib/application/runtime/call_media_session_coordinator.dart` now owns app-side audio/video media adapter classes and media diagnostics mapping. `VoiceCallRuntime` still owns command orchestration, room reconciliation, lock coordination, state mutation, and terminal cleanup; continue Phase 3 by adding characterization around terminal room/late Firebase update paths, then extract room reconciliation and lock coordination. Validation executed: `flutter test test\call_error_classifier_test.dart --reporter expanded`, `flutter test test\voice_call_runtime_media_path_test.dart test\voice_call_runtime_diagnostics_contract_test.dart --reporter expanded`, `flutter test test\rain_call_failure_messages_test.dart --reporter expanded`, `dart run melos run analyze`, full `dart run melos run test`, and `.\scripts\check_obsidian_vault.ps1` passed.
- 2026-06-05 senior audit Phase 1 local proof is implemented. Chat/connect/call/offline-request action authority now flows through runtime-backed `PeerConnectivitySnapshot` instead of local `friend.isOnline`; snapshots carry backend presence session id, heartbeat age, presence state, freshness window, and observation time. `ChatPanel` watches the selected immutable peer snapshot and uses `peerOnlineForAction` for call/connect/request preflight. RTDB offline request preflight uses the 45 second rules freshness window. Validation executed: `dart pub get`, `dart run melos run analyze`, full `dart run melos run test`, focused `flutter test test\peer_connectivity_provider_test.dart --reporter expanded`, and `.\scripts\check_obsidian_vault.ps1` passed. Connection-request quota proof remains tracked separately under BLK-009.
- 2026-06-05 senior audit Phase 2 Firebase call lock/rule proof is complete locally. Added Firebase emulator assertions for malformed voice lock/inbox write denial, post-denial state preservation, live-lock preservation, terminal leftover lock reclamation, missing inbox cleanup, unauthorized transition denial, oversized terminal reason denial, and server-authoritative voice lock transactions. The first full emulator script run failed because the new test read Bob's protected inbox with Alice's auth token; the helper was fixed and the rerun passed. Validation executed: `flutter test test\firebase_contract_test.dart --reporter expanded`, `.\scripts\ci_run_firebase_emulators.ps1`, backend functions `npm run lint`, `npm test`, `npm run audit:moderate`, `dart run melos run analyze`, full `dart run melos run test`, and vault validation passed. Device media-direction proof remains Phase 10 work.
- 2026-06-05 false busy/stale Firebase voice lock remediation is implemented and locally release-gate validated. `packages/protocol_brain/lib/src/voice_lock_reclaim_policy.dart` now owns the shared decision policy for keep-busy vs reclaim-lock vs reclaim-lock-and-delete-room-artifacts. `FirebaseSignalingAdapter.createOutgoingCall` routes pair/user lock conflicts through that policy, uses compare-delete before reclaiming, retries the claim once, and reports retryable cleanup when old state was cleaned but a newer lock wins. `FakeVoiceSignalingAdapter` uses the same policy. A related `VoiceCallRuntime` fix now ignores locally initiated terminal Firebase room echoes while the latched local hangup is still ending/ended, so the original hangup path still awaits session/media cleanup. Validation executed: focused stale-lock tests passed, the previously failing hangup/media cleanup cases passed, `dart run melos run analyze` passed, full `dart run melos run test` passed, and `.\scripts\check_obsidian_vault.ps1` passed.
- 2026-06-05 AI tooling overlay is installed in the active Rain repo at `C:\Users\eslam\OneDrive\Desktop\GoodStuff\Rain`. The overlay adds `.ai/` guidance, `scripts/ai/` helper commands, and a marked `AGENTS.md` block for Context7, OpenViking, Promptfoo, Impeccable, and agency-role routing. It does not change app runtime code, Flutter/Firebase dependencies, CI gates, hooks, or deployment behavior. Promptfoo remains opt-in because Rain has AI operating docs but no runtime LLM behavior.
- 2026-06-04 Phase E Android splash flash mitigation is implemented and locally validated. `values/styles.xml` and `values-night/styles.xml` now set both `LaunchTheme` and `NormalTheme` `android:windowBackground` to `@drawable/launch_background`; default and v21 launch drawables remain dark `#061017`. Added `apps/rain/test/android_splash_resources_test.dart`; `flutter test test/android_splash_resources_test.dart --reporter expanded` passed from `apps/rain`. Next requested phase is Phase C shutdown split/budget.
- 2026-06-04 multi-issue stability investigation is documented in [[2026-06-04 Multi-Issue Stability Investigation]]. Scope was investigation/planning only; no production code changed. The dominant finding is split runtime/provider/presence/call state ownership. Recommended next implementation starts with an authoritative peer/call capability snapshot, then local call-ended presentation before cleanup waits, shutdown critical-vs-best-effort split, desktop video preflight, Android splash theme alignment, and gender-avatar data-path proof.
- 2026-06-04 update warning metadata fix is implemented, locally validated, pushed, and cloud release-gate proven. Root cause: the app package, deployable Remote Config template, and release manifest example all advertised `1.0.6+7`, so installed `1.0.6+7` apps correctly evaluated update status as `current` and showed no warning. The app is now bumped to `1.0.7+8`; `backend/firebase/remoteconfig.template.json` and `docs/releases/rain_release_manifest_v1.example.json` advertise `1.0.7+8`; `version_metadata_test.dart` now proves the checked-in Remote Config template reports `updateRequired` for previous `1.0.6+7` Android/Windows stable/demo installs. Validation executed: the regression failed before the metadata bump, then passed after the bump; release contract test passed. `Build Rain Apps` run 26963049075 passed on pushed `dev` SHA `f1904e72f1c16773700f0bfa6bcc8ac0fcd7706d` and published `rain-test-109-1`. Remote Config deployment was not executed.
- 2026-06-04 terminal call cleanup and Windows close mitigation is implemented and locally validated from `rain-diagnostics-2026-06-04T144952-237539Z.json`. The diagnostic showed a Firebase terminal `busy` room after media had reached `connected`, a local stale active call state that blocked file transfer, and app shutdown starting without completion. `VoiceCallRuntime` now publishes terminal failed/idle state before bounded WebRTC/session cleanup, clears stale call resources before awaiting disposal, bounds cleanup steps, and keeps late cleanup from reactivating UI state. `DesktopShellController` now bounds close/destroy steps and exits the Windows process after close fallback. Validation executed: targeted call/file/shutdown contract tests, targeted terminal `friend_flow_test.dart` cases, `dart run melos run analyze`, and full `dart run melos run test` passed.
- 2026-06-04 scenario-intelligence operating layer was added and hardened in the Obsidian vault. New graph notes now define [[System Model]], [[State Graph]], [[Business Rule Graph]], [[Assumption Register]], [[Failure Graph]], [[Scenario Intelligence Agent]], and [[Scenario Coverage Matrix]]. Future testing/intelligence agents should derive scenarios by violating assumptions, tracing state/business-rule/failure graph edges, mapping coverage status, and recording findings in risks, debt, blockers, tests, or next actions.
- 2026-06-04 auth scenario release-gate integration is implemented and cloud proven. `build-artifacts.yml` and `validated-release.yml` run explicit `SCN-AUTH-001` through `SCN-AUTH-004` app tests, both hard release workflows run Obsidian vault validation, and the Firebase emulator runner includes `integration_account_deletion_emulator_test.dart` for tombstone cleanup plus surviving-Auth no-recreate proof. Local `.\scripts\ci_run_firebase_emulators.ps1` passed after this change. `Build Rain Apps` run 26957834309 passed on pushed `dev` SHA `883886a2e81ee370e3641ddcefce8d62942a3566` and published `rain-test-108-1`.
- 2026-06-04 cloud release-gate proof passed for pushed `origin/dev` SHA `d58b7b5`: GitHub `Build Rain Apps` run 26931788461 passed the `Hard Release Gate`, Android v7/v8-v9 APK builds, Windows demo portable build, and `rain-test-107-1` pre-release publication. This earlier proof was superseded for account-deletion/scenario-gate work by run 26957834309 at `883886a`.
- 2026-06-04 account deletion workflow is implemented and validated locally. Settings now exposes a destructive delete-account flow with password reauthentication; `SignalingAdapter`/`FirebaseSignalingAdapter` own reauth and account deletion; backend deletion tombstones `users/{username}`, removes `userSearch` best-effort, clears relationship/request/block mirrors where rules allow best-effort, sets presence offline best-effort, ends active calls best-effort, and deletes Firebase Auth last. Bad-password reauth failures and required tombstone failures do not clear the current local identity/session; successful deletion or post-tombstone destructive failure clears local state. Follow-up hardening prevents login, identity upsert, or `userSearch` writes from reviving a missing/tombstoned backend account after Auth succeeds. Validation executed: `dart run melos run analyze` passed; `dart run melos run test` passed for `peer_core`, `protocol_brain`, `rain`, and `rain_core`; targeted auth/source-of-truth app test passed through the app test wrapper.
- 2026-06-04 Phase 2 discovery for the current handoff request completed before implementation. It found no first-class account deletion UI, adapter contract, Firebase adapter method, or delete-account tests at that time; the later 2026-06-04 account deletion implementation supersedes that discovery gap.
- 2026-06-04 governance/operating-system synchronization is complete. Root `AGENTS.md`, `PROJECT_OPERATING_SYSTEM.md`, and Obsidian AI operating notes now encode the Rain Autonomous Engineering System priority order, reality-enforcement rules, and workflow node state machine. Vault validation passed for this documentation/governance change. No application code, Firebase rules, database schema, or WebRTC behavior changed.
- 2026-06-04 registration permission-denied investigation is complete for the supplied create-account screenshot. Live Firebase checks showed current rules allow fresh random registration, while `/users/eslam` already exists. The app now maps RTDB permission-denied during registration to an account conflict message, rolls back the just-created Auth user only before the durable user row exists, signs out without caching Drift identity on backend-save failures, and includes targeted auth/onboarding regression tests.
- 2026-06-04 terminal/media-write and diagnostics-export follow-up is implemented and awaiting validation. The supplied Windows diagnostic showed `signaling.writeVoiceOffer` throwing after the Firebase room was already `ended`; runtime now preflights terminal-sensitive media signaling sends before `accept`, `offer`, `answer`, and `mute` writes. Firebase rules contract tests now assert voice terminal fields are writable by either participant. Diagnostics export now reports a real fallback JSON file when Android returns a content URI or `/document/...` picker handle. File-transfer views now reset speed samples when the peer lane is not connected.
- `ROOT_CAUSE_ANALYSIS.md` was created on 2026-06-03 from the supplied Windows diagnostics JSON, Android screenshot, and manual failure report. It is the current evidence lock for call/presence/update/diagnostics failures.
- The RCA confirmed these root-cause clusters: split call terminal authority, Android `signaling.endCall` permission denial, presence freshness races, terminal inbox exposure before cleanup, Android diagnostics export path failure, and update build-number inconsistency.
- First mitigation from the RCA execution is complete: late voice signaling states after terminal Firebase rooms are recorded as `late_frame_ignored` events only and no longer overwrite crash diagnostics as `lastCrash`.
- Second mitigation from the RCA execution is complete: Firebase `endCall` now writes the terminal `voiceCalls/{callId}` room state before any best-effort inbox mirror update, so an already-cleaned `voiceCallInboxes/{callee}/{callId}` row can no longer cause a permission-denied terminal cleanup failure.
- Third mitigation from the RCA execution is complete: diagnostics export treats Android SAF `/document/...` and `/tree/...` handles as platform-managed picker outputs and no longer opens them as raw filesystem paths.
- Fourth mitigation from the RCA execution is complete: backend presence is resolved from `online + lastHeartbeat` with a shared 30 second runtime freshness window before local friend seeding, direct connect, connection-request routing, or voice/video call start. Stale raw-online records are treated as offline and logged as `backend_presence_stale_resolved_offline`.
- Fifth mitigation from the RCA execution is complete: call setup diagnostics now retain a per-call Firebase room status timeline and terminal-room failure reconciliation emits `VoiceCallDiagnostics`, so failed setup exports can show ringing/accepted/failed transitions instead of an empty room timeline.
- Sixth mitigation from the RCA execution is complete: update checks now classify stale Remote Config release policy as `remotePolicyOutdated` instead of `current`, same-version minimum build upgrades become required updates, and optional update prompts render from the root app surface before login/home.
- Seventh mitigation from the RCA execution is complete: Phase 05 presence/session freshness now carries session metadata through backend identity snapshots, treats `presence.state != online` as offline, blocks UI Connect and auto-recovery through the shared fresh-presence resolver, and preserves `presenceExpired` as a terminal peer intent until a later successful explicit reconnect.
- Phase 08 Regression Test Expansion is complete: targeted local tests now cover stable failure messages for WebRTC/Firebase/network call failures, failed call suite state and compact video dock behavior, terminal-room-before-session-hangup ordering, failed-media terminal write before session disposal, already-terminal cleanup classification, and session-owned Firebase presence contracts.
- Phase 09 local pre-artifact release gate evidence is recorded: `dart pub get`, analyze, full Melos tests, Firebase JSON parsing, Firebase Functions tests, Firebase emulator integration tests, and Obsidian vault validation all passed on 2026-06-03. Cloud artifact build and release-page proof remain the hard-gate completion step.
- Auth/startup remediation Phase 1 is complete on 2026-06-03. `apps/rain/lib/application/state/identity_providers.dart` validates cached identity through backend account existence and current auth uid before publishing signed-in state, clears local session on missing/deleted/mismatched backend identity, and saves local identity only after backend identity/presence writes during login/register. `apps/rain/test/auth_identity_source_of_truth_test.dart` covers backend deletion, uid mismatch, and backend profile refresh.
- Auth/startup remediation Phase 2 is complete on 2026-06-03. `RainRuntimeController.logOut()` now guarantees local Drift session clearing before best-effort backend sign-out, even if `adapter.signOut()` fails or a previous app-exit shutdown already owns `_shutdownFuture`. `RuntimeController.logOut()` now ends the authenticated session from `finally`; Phase 6 replaced the earlier broad provider invalidation list with generation-scoped providers. `apps/rain/test/runtime_startup_test.dart` covers failed sign-out and logout-after-app-exit shutdown.
- Auth/startup remediation Phase 3 is complete on 2026-06-03. `apps/rain/lib/application/state/app_startup_state.dart` centralizes startup phases, `RootScreen` and `appShellReadinessProvider` now derive readiness from it, router refresh listens to startup state, and `apps/rain/test/app_routes_test.dart` covers update loading, required update, session validation, signed-out, runtime loading, ready, and session-expired phases.
- Auth/startup remediation Phase 4 is complete on 2026-06-03. `RainApp` now places `RainStartupSurface` above the routed child through `MaterialApp.router.builder` whenever `AppStartupState.blocksRoutedSurface` is true, so loading, required-update, failed, and session-expired states do not insert `RainNavigationShell` or normal app chrome. `RootScreen` reuses the same startup surface for route-local consistency, and route tests prove no shell/navigation exists while startup is blocked.
- Auth/startup remediation Phase 5 is complete on 2026-06-03. `AppStartupState.canRenderProtectedRoutes` and `usesRoutedAppShell` now make the protected route contract explicit; `RainApp` renders signed-out auth through a standalone Navigator/Overlay instead of the app shell; settings/search/friend pages use `_ProtectedRouteGate`; router redirects unresolved protected paths to `/`; tests prove protected routes do not render while runtime is loading or signed out.
- Auth/startup remediation Phase 6 is complete on 2026-06-03. `AuthenticatedSession.sessionGeneration` is the account-scope boundary, runtime reuse requires matching username and generation, account-owned providers watch the active generation, logout ends the authenticated session instead of relying on a broad manual invalidation list, and regression tests cover generation changes plus recent/search/message state reset.
- Local Windows Drift/sqlite app-test invocation is repaired for isolated Rain app tests: `scripts/run_rain_app_test.ps1` runs tests from `apps/rain`, the targeted stale-presence friend-flow case passed, and full `friend_flow_test.dart` passed with 120 passing tests and 10 skipped legacy control-channel cases.
- Phase 0 deliverables are complete.
- Phase 1 vault structure is complete.
- Phase 2 repository discovery is documented in `obsidian-vault/03-Architecture/Current Architecture.md`.
- Phase 3 primary AI memory is documented in `obsidian-vault/AI-Memory/Project Memory.md`.
- Phase 4 roadmap artifacts are documented in `obsidian-vault/01-Roadmap/`.
- Phase 5 technical debt artifacts are documented in `obsidian-vault/11-Technical Debt/`.
- Phase 6 risk and blocker artifacts are documented in `obsidian-vault/12-Risks/` and `obsidian-vault/14-Blockers/`.
- Phase 7 architecture refactor plans are documented in `obsidian-vault/03-Architecture/`.
- Phase 8 self-improvement artifacts are documented in `obsidian-vault/18-Lessons Learned/`.
- Repository-wide `AGENTS.md` now requires pre-implementation reading of project memory, roadmap, debt, risk, and blockers, plus a post-code Obsidian update gate.
- `obsidian-vault/01-Roadmap/Engineering System Flaw Remediation Plan.md` has been created to fix flaws in the current documentation operating system before Phase 9 automation.
- Engineering System Flaw Remediation Phase 00 and Phase 01 are complete: canonical source views are documented, duplicate note titles were removed, and the vault checker now fails on uncontrolled duplicate note titles.
- Continue remediation with one logical phase per commit. Do not perform another audit unless explicitly requested.

## Known Risks

- The repository already contains prior documentation and an expanded Obsidian vault; future phases must avoid duplicating or contradicting it.
- The active maintained repo must remain separate from `D:\old project\Rain`.
- The project has high-risk runtime areas: Firebase signaling, WebRTC calls, presence, update checks, diagnostics, and release workflows.
- Future phases must avoid overbuilding before discovery.
- The vault now has local semantic validation for operational status schemas, evidence ledgers, and stale review dates. Generated metric reconciliation is still manual and should be automated before treating metrics as fully machine-checked.

## Known Blockers

- BLK-002 is mitigated for the implemented false-busy stale-lock policy and local Firebase emulator proof now covers stale/terminal/malformed/live-lock preservation paths. It remains open only for live Firebase or device-direction release evidence if required by the release gate.
- BLK-010 is mitigated and release-gate proven for auth/session/account-deletion behavior after `Build Rain Apps` run 26957834309, but remains open for broader production readiness until remaining startup/device proof and cross-flow scenario gaps are closed.

## Next Recommended Action

Use [[Scenario Coverage Matrix]] as the next planning surface: run Phase 10 device/media proof (`device_media_reality_proof_test.dart` on a real Android endpoint), then continue Phase 3 extraction for call command and lease orchestration now that media/session-state/signaling cleanup delegates are split from `VoiceCallRuntime` and the runtime extension files are proper imports.

## Future Population Areas

Future phases will populate:

- Architecture
- Risks
- Roadmaps
- Tasks
- Metrics
- Knowledge graph
- Self-improvement data
- Lessons learned
- Technical debt
- Blockers
- ADRs
