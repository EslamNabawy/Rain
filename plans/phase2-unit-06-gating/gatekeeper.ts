// Lightweight gating stubs for Phase 2
export function gateCodeReview(unit: string, changes: any): boolean {
  // Placeholder: always pass for now
  return true;
}

export function gateSecurity(unit: string, changes: any): boolean {
  // Placeholder: always pass for now
  return true;
}
