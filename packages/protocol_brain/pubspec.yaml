name: protocol_brain
description: Signaling, sessions, and reconnect logic for Rain.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.10.4
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_auth: ^6.1.1
  firebase_database: ^12.0.4
  collection: ^1.19.1
  flutter_webrtc: ^0.14.2
  peer_core:
    path: ../peer_core
  supabase_flutter: ^2.10.3

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
